
<h1>this is the app/temp/index.hbs</h1>


<ul>
  {{#each model as |article|}}
  <li>{{article.author}} {{article.image}} {{article.title}}
  <p>{{#link-to 'article' article.id}}<img src={{article.image}} alt={{article.title}}>{{/link-to}}</p>
  </li>
  {{/each}}
</ul>

{{#link-to 'admin'}}Admin{{/link-to}}
